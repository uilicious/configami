//----------------------------------------------------------------
//
// This file is system generated to load your template
// within the cache - this file should not be modified directly
//
//----------------------------------------------------------------

/**
 * Runs the template function with the given context
 * @param {TemplateContext} context 
 */
function _templateFunction( context ) {
	// Unwrap the context object
	// const { output, input, moduleRoot, fsHelper } = context;

	// Copy over with templates, various files
	context.applyTemplate();
	
}
module.exports = _templateFunction;